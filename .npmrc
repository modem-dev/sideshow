# Require new package versions to spend at least 7 days on the registry before
# npm can resolve them. This reduces exposure to dependency-confusion and
# freshly-published malware while preserving installs from package-lock.json.
min-release-age=10080

# Keep installs deterministic and fail loudly on known vulnerable packages.
package-lock=true
audit=true
fund=false
