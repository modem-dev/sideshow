---
"sideshow": minor
---

Ship the multi-part surfaces release: markdown, terminal, diff, image, trace, and upload parts across the CLI, MCP, and HTTP APIs; a stable `sideshow/server` integration entrypoint; exact-once `sideshow watch` feedback delivery; the Claude Code plugin and viewer connect panel; recency-grouped sessions and comment-focused copy; the snippets-to-surfaces API migration; standalone `sideshow-term` packaging; and hardening for terminal rendering, STML parsing, storage cold-loads, unknown viewer parts, and malformed surface payloads.
